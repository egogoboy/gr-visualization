id: visualization_message_generator
label: Message generator
category: '[visualization]'

templates:
  imports: from gnuradio import visualization
  make: visualization.message_generator()

parameters:
- id: message frequency
  name: Message frequency
  dtype: float
  default: 10.0
  type: float
  unit: Hz
  tooltip: Messages frequency

inputs: []

outputs:
- id: out
  domain: message

file_format: 1
